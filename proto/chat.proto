syntax = "proto3";
package chat;

service Chat {
  // ChatStream is a bidirectional streaming RPC for exchanging chat messages
  rpc ChatStream(stream ChatMessage) returns (stream ChatMessage);
}

message ChatMessage {
  string username = 1;
  string message = 2;
}
